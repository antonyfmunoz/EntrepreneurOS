---
phase: 05-backend-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/backend-wirer/types.ts
  - lib/backend-wirer/brownfield-backend-audit.ts
  - tests/unit/backend-wirer/brownfield-backend-audit.test.ts
autonomous: true
requirements: [BACK-01, BACK-05]

must_haves:
  truths:
    - "BackendBrownfieldInventory captures all existing route paths from routes.ts"
    - "BackendBrownfieldInventory captures all existing storage function names from storage.ts"
    - "BackendBrownfieldInventory captures all existing table names from schema.ts"
    - "BackendBrownfieldInventory identifies the exact insertion offset before createServer(app)"
    - "Collision detection flags BackendEndpointSpec paths that match existing routes"
  artifacts:
    - path: "lib/backend-wirer/types.ts"
      provides: "Type contracts for all Phase 5 backend wiring modules"
      exports: ["BackendBrownfieldInventory", "BackendBrownfieldInventorySchema", "BackendWiringPlan", "BackendWiringPlanSchema", "RouteCodeBlock", "SchemaCodeBlock", "StorageCodeBlock", "HookInjection", "WiringValidationResult"]
    - path: "lib/backend-wirer/brownfield-backend-audit.ts"
      provides: "Scans existing backend files for collision detection"
      exports: ["auditBackendBrownfield"]
    - path: "tests/unit/backend-wirer/brownfield-backend-audit.test.ts"
      provides: "Unit tests for brownfield backend audit"
  key_links:
    - from: "lib/backend-wirer/types.ts"
      to: "@shared/spec-schema.ts"
      via: "imports BackendSpec, BackendEndpointSpec types"
      pattern: "import.*BackendSpec.*from.*@shared/spec-schema"
    - from: "lib/backend-wirer/brownfield-backend-audit.ts"
      to: "lib/backend-wirer/types.ts"
      via: "returns BackendBrownfieldInventory"
      pattern: "BackendBrownfieldInventory"
---

<objective>
Define all Phase 5 type contracts and build the backend brownfield audit module.

Purpose: Every downstream Phase 5 module (route-generator, schema-generator, hook-injector, test-generator, fix-loop) depends on these types. The brownfield audit prevents collision with the existing 40+ endpoints in routes.ts, existing CRUD functions in storage.ts, and existing tables in schema.ts. Per D-01, the BackendSpec from deriveBackendSpec() is the source of truth — no TSX scanning. Per D-04, brownfield scanning must happen before any code generation.

Output: Type contracts in lib/backend-wirer/types.ts, brownfield audit function in lib/backend-wirer/brownfield-backend-audit.ts, unit tests confirming audit correctness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-quality/05-CONTEXT.md
@.planning/phases/05-backend-quality/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From shared/spec-schema.ts (lines 117-139):
```typescript
export const BackendEndpointSpec = z.object({
  method: z.enum(["GET", "POST", "PUT", "PATCH", "DELETE"]),
  path: z.string().min(1).regex(/^\//, "Endpoint path must start with /"),
  description: z.string(),
  requestBody: z.array(z.string()).default([]),
  responseFields: z.array(z.string()).default([]),
  authRequired: z.boolean().default(true),
  uiPageRef: z.string().optional(),
  source: SpecItemSource.default("inferred"),
});
export type BackendEndpointSpec = z.infer<typeof BackendEndpointSpec>;

export const BackendSpecSchema = z.object({
  endpoints: z.array(BackendEndpointSpec),
  drizzleTableHints: z.array(z.string()).default([]),
  backgroundJobs: z.array(z.string()).default([]),
  mismatches: z.array(z.string()).default([]),
});
export type BackendSpec = z.infer<typeof BackendSpecSchema>;
```

From lib/code-integrator/brownfield-audit.ts (Phase 4 pattern — extend to backend):
- `auditBrownfield(projectRoot: string): Promise<BrownfieldInventory>` — scans client files
- Uses regex to extract routes, pages, nav items from TSX files
- Returns Zod-validated inventory object

From server/routes.ts:
- Line 2524: `const httpServer = createServer(app);` — insertion point
- Route pattern: `app.get("/api/agents/:id/metrics", async (req, res) => {`

From server/storage.ts:
- Class-based singleton with `async functionName(` pattern
- Exports: `export const storage = new DatabaseStorage();`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase 5 type contracts</name>
  <files>lib/backend-wirer/types.ts, tests/unit/backend-wirer/types.test.ts</files>
  <read_first>
    - shared/spec-schema.ts (BackendEndpointSpec, BackendSpec, SpecOutput types — lines 115-153)
    - lib/code-integrator/types.ts (Phase 4 pattern for type file structure with Zod schemas)
    - lib/spec-parser/types.ts (re-export pattern)
  </read_first>
  <behavior>
    - BackendBrownfieldInventorySchema validates an object with existingRoutePaths string[], existingStorageFunctions string[], existingTableNames string[], routesInsertionOffset number, storageInsertionOffset number, schemaInsertionOffset number
    - BackendWiringPlanSchema validates an object with newRoutes RouteCodeBlock[], newSchemaBlocks SchemaCodeBlock[], newStorageFunctions StorageCodeBlock[], hookInjections HookInjection[], validationResult WiringValidationResult
    - RouteCodeBlock has method string, path string, code string, zodSchemaCode string|undefined
    - SchemaCodeBlock has tableName string, drizzleCode string, zodInsertCode string, typeExportCode string
    - StorageCodeBlock has functionName string, code string, tableName string
    - HookInjection has pageFilePath string, hookImport string, hookCode string, replacePattern string
    - WiringValidationResult has valid boolean, gaps string[] (data requirements with no endpoint), collisions string[] (new endpoints matching existing routes)
    - Empty gaps array + empty collisions array = valid: true
    - Non-empty gaps or collisions = valid: false
  </behavior>
  <action>
    Create lib/backend-wirer/types.ts with all Phase 5 type contracts as Zod schemas + inferred TypeScript types. Follow the exact pattern from lib/code-integrator/types.ts (Zod schema first, then `type X = z.infer<typeof XSchema>`).

    Types to define (per D-01, D-03, D-04, D-06, D-07, D-08, D-09, D-15):

    1. **BackendBrownfieldInventorySchema** (per D-04 — collision detection):
       - `existingRoutePaths: z.array(z.string())` — all `/api/...` paths from routes.ts
       - `existingStorageFunctions: z.array(z.string())` — all async method names from storage.ts
       - `existingTableNames: z.array(z.string())` — all pgTable names from schema.ts
       - `routesInsertionOffset: z.number()` — byte offset before `createServer(app)` in routes.ts
       - `storageInsertionOffset: z.number()` — byte offset before closing `}` of class in storage.ts
       - `schemaInsertionOffset: z.number()` — byte offset at end of schema.ts

    2. **RouteCodeBlock** (per D-06, D-08, D-09):
       - `method: z.string()`, `path: z.string()`, `code: z.string()`, `zodSchemaCode: z.string().optional()`

    3. **SchemaCodeBlock** (per D-15):
       - `tableName: z.string()`, `drizzleCode: z.string()`, `zodInsertCode: z.string()`, `typeExportCode: z.string()`

    4. **StorageCodeBlock** (per D-07):
       - `functionName: z.string()`, `code: z.string()`, `tableName: z.string()`

    5. **HookInjection** (per D-02):
       - `pageFilePath: z.string()`, `hookImport: z.string()`, `hookCode: z.string()`, `replacePattern: z.string()`

    6. **WiringValidationResult** (per D-03):
       - `valid: z.boolean()`, `gaps: z.array(z.string())`, `collisions: z.array(z.string())`

    7. **BackendWiringPlanSchema** — composite:
       - `newRoutes: z.array(RouteCodeBlockSchema)`, `newSchemaBlocks: z.array(SchemaCodeBlockSchema)`, `newStorageFunctions: z.array(StorageCodeBlockSchema)`, `hookInjections: z.array(HookInjectionSchema)`, `validationResult: WiringValidationResultSchema`

    Import BackendSpec and BackendEndpointSpec from `@shared/spec-schema.js` and re-export them for convenience.

    Create tests/unit/backend-wirer/types.test.ts with Zod parse/safeParse validation tests for all schemas (valid data passes, invalid data fails with correct error).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/backend-wirer/types.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - lib/backend-wirer/types.ts contains `export const BackendBrownfieldInventorySchema = z.object(`
    - lib/backend-wirer/types.ts contains `export const BackendWiringPlanSchema = z.object(`
    - lib/backend-wirer/types.ts contains `export const RouteCodeBlockSchema = z.object(`
    - lib/backend-wirer/types.ts contains `export const SchemaCodeBlockSchema = z.object(`
    - lib/backend-wirer/types.ts contains `export const StorageCodeBlockSchema = z.object(`
    - lib/backend-wirer/types.ts contains `export const HookInjectionSchema = z.object(`
    - lib/backend-wirer/types.ts contains `export const WiringValidationResultSchema = z.object(`
    - lib/backend-wirer/types.ts contains `export type BackendBrownfieldInventory = z.infer<`
    - lib/backend-wirer/types.ts contains `export type BackendWiringPlan = z.infer<`
    - lib/backend-wirer/types.ts contains `import.*BackendSpec.*from.*@shared/spec-schema`
    - tests/unit/backend-wirer/types.test.ts exits 0
    - At least 7 test cases (one per schema)
  </acceptance_criteria>
  <done>All 7 Zod schemas defined, typed, and exported. Test file validates all schemas with both valid and invalid inputs. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend brownfield audit</name>
  <files>lib/backend-wirer/brownfield-backend-audit.ts, tests/unit/backend-wirer/brownfield-backend-audit.test.ts</files>
  <read_first>
    - lib/backend-wirer/types.ts (Task 1 output — BackendBrownfieldInventory schema)
    - lib/code-integrator/brownfield-audit.ts (Phase 4 pattern — regex scanning approach)
    - server/routes.ts (first 30 lines + last 30 lines — see route pattern and createServer insertion point)
    - server/storage.ts (first 30 lines + last 30 lines — see class structure and function pattern)
    - shared/schema.ts (first 30 lines — see pgTable pattern)
  </read_first>
  <behavior>
    - auditBackendBrownfield with routes content containing `app.get("/api/users", ...)` and `app.post("/api/agents", ...)` returns existingRoutePaths including "/api/users" and "/api/agents"
    - auditBackendBrownfield with storage content containing `async getUser(` and `async createAgent(` returns existingStorageFunctions including "getUser" and "createAgent"
    - auditBackendBrownfield with schema content containing `pgTable("users",` and `pgTable("agents",` returns existingTableNames including "users" and "agents"
    - routesInsertionOffset equals the index of "const httpServer = createServer(app)" in the routes content
    - When routes content has no createServer call, routesInsertionOffset is -1
    - detectCollisions returns collisions for BackendEndpointSpec paths that match existing route paths
    - detectCollisions returns gaps for data requirements with no corresponding endpoint
  </behavior>
  <action>
    Create lib/backend-wirer/brownfield-backend-audit.ts with two exported functions:

    1. **`auditBackendBrownfield(projectRoot: string): Promise<BackendBrownfieldInventory>`**
       - Read `server/routes.ts`, `server/storage.ts`, `shared/schema.ts` using `fs/promises readFile`
       - Extract existing route paths: regex `/app\.(get|post|put|patch|delete)\("(\/api\/[^"]+)"/g` — capture group 2
       - Extract existing storage functions: regex `/async (\w+)\s*\(/g` — capture group 1
       - Extract existing table names: regex `/pgTable\("([^"]+)"/g` — capture group 1
       - Find routesInsertionOffset: `routesContent.indexOf("const httpServer = createServer(app)")` — returns byte offset or -1
       - Find storageInsertionOffset: `storageContent.lastIndexOf("}")` — last closing brace of class
       - Find schemaInsertionOffset: `schemaContent.length` — append at end of file
       - Validate result against BackendBrownfieldInventorySchema before returning

    2. **`detectCollisions(inventory: BackendBrownfieldInventory, spec: BackendSpec): WiringValidationResult`**
       - Per D-03: Cross-check every BackendEndpointSpec.path against inventory.existingRoutePaths
       - Collisions: spec endpoint paths that already exist in inventory.existingRoutePaths (exact string match, also check with/without trailing parameterized segments like `:id`)
       - Gaps: not applicable here (gaps are checked at wiring plan generation time, not audit time)
       - Return `{ valid: collisions.length === 0, gaps: [], collisions }`

    Import types from `./types.js`. Use `join` from `path` for file path construction. Follow kebab-case file naming per CLAUDE.md conventions.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/backend-wirer/brownfield-backend-audit.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - lib/backend-wirer/brownfield-backend-audit.ts contains `export async function auditBackendBrownfield(`
    - lib/backend-wirer/brownfield-backend-audit.ts contains `export function detectCollisions(`
    - lib/backend-wirer/brownfield-backend-audit.ts contains `import.*BackendBrownfieldInventory.*from.*\.\/types`
    - lib/backend-wirer/brownfield-backend-audit.ts contains `routesContent.indexOf("const httpServer = createServer(app)")`
    - lib/backend-wirer/brownfield-backend-audit.ts contains regex pattern `app\.\(get\|post\|put\|patch\|delete\)` or equivalent
    - lib/backend-wirer/brownfield-backend-audit.ts contains regex pattern `pgTable\(` or equivalent
    - tests/unit/backend-wirer/brownfield-backend-audit.test.ts exits 0
    - At least 6 test cases covering: route extraction, storage function extraction, table name extraction, insertion offset detection, collision detection with match, collision detection with no match
  </acceptance_criteria>
  <done>auditBackendBrownfield reads three backend files and returns a validated inventory. detectCollisions cross-checks BackendSpec against inventory. All tests pass with at least 6 test cases.</done>
</task>

</tasks>

<verification>
npx vitest run tests/unit/backend-wirer/ --reporter=verbose
</verification>

<success_criteria>
- lib/backend-wirer/types.ts exports all 7 Zod schemas and their inferred types
- lib/backend-wirer/brownfield-backend-audit.ts exports auditBackendBrownfield and detectCollisions
- All unit tests in tests/unit/backend-wirer/ pass
- No TypeScript compilation errors under strict mode
</success_criteria>

<output>
After completion, create `.planning/phases/05-backend-quality/05-01-SUMMARY.md`
</output>
