---
phase: 04-code-integration
plan: 04
type: execute
wave: 1
depends_on: ["04-03"]
files_modified: []
autonomous: true
gap_closure: true
requirements: [INTG-01, INTG-02, INTG-03, INTG-04, INTG-05, GIT-01, GIT-02, GIT-03]

must_haves:
  truths:
    - "feature/company-system branch is pushed to origin with all 76 local commits"
    - "A pull request exists on GitHub for the Phase 4 code integration work"
  artifacts:
    - path: "origin/feature/company-system"
      provides: "Remote branch with all Phase 4 commits"
    - path: "GitHub PR"
      provides: "PR-ready state for Phase 4 deliverables"
  key_links:
    - from: "local feature/company-system"
      to: "origin/feature/company-system"
      via: "git push -u origin feature/company-system"
      pattern: "git push"
    - from: "origin/feature/company-system"
      to: "GitHub PR"
      via: "gh pr create"
      pattern: "gh pr create"
---

<objective>
Close the single remaining Phase 4 verification gap: push the feature/company-system branch to remote and open a PR on GitHub.

Purpose: Phase 4 success criterion 4 requires "the branch is pushed to remote in PR-ready state." The pushAndCreatePR module is fully implemented and tested (10 tests pass), but the actual branch carrying Phase 4 work was never pushed. This is a delivery execution gap, not a capability gap.

Output: Remote branch updated, GitHub PR created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-integration/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push branch and create PR</name>
  <files></files>
  <read_first>
    - .planning/phases/04-code-integration/04-VERIFICATION.md (confirms gap details)
  </read_first>
  <action>
    1. Push the feature/company-system branch to origin:
       `git push -u origin feature/company-system`
       This will push all 76 local commits that are ahead of origin.

    2. Create a GitHub PR using the gh CLI. Target branch: main.
       Title: "feat: Phase 4 — brownfield-aware code integration pipeline"
       Body should summarize Phase 4 deliverables:
       - Brownfield audit module (auditBrownfield) — scans existing codebase before writing files
       - HTML-to-shadcn translation (translateHtmlToShadcn) — Claude AI rewrites Stitch HTML to use existing shadcn/ui components
       - Page writer (writePage) — creates page files with conflict detection
       - Route injector (injectRoute) — wires ProtectedRoute + CompanyGate into App.tsx
       - Nav injector (injectNavItem) — adds sidebar entries with remixicon icons
       - Git workflow (createBranch, commitPage, pushAndCreatePR, detectBaseBranch) — branch lifecycle management
       - Integrator SKILL.md — orchestrates the full per-page pipeline
       - 44 unit tests across 6 test files, all passing

       Use `gh pr create --base main --title "..." --body "..."` format.

    3. Verify the push succeeded by running `git log origin/feature/company-system..HEAD` — should show 0 commits ahead.

    4. Verify the PR exists by running `gh pr view --json number,url,state` — should show an open PR.

    Per D-15: Auto-push after all pages, auto-create PR.
    Closes GIT-03 verification gap.
  </action>
  <verify>
    <automated>cd "C:/Users/antonys beast pc/dev/EntrepreneurOS" && git log origin/feature/company-system..HEAD --oneline | wc -l | tr -d ' ' && gh pr list --head feature/company-system --json number,url --jq '.[0].url'</automated>
  </verify>
  <acceptance_criteria>
    - `git log origin/feature/company-system..HEAD` returns 0 commits (branch fully pushed)
    - `gh pr list --head feature/company-system` returns at least one open PR
    - PR targets main branch
    - PR title and body describe Phase 4 code integration work
  </acceptance_criteria>
  <done>feature/company-system is pushed to origin with 0 commits ahead, and a GitHub PR exists in open state targeting main</done>
</task>

</tasks>

<verification>
1. `git log origin/feature/company-system..HEAD --oneline | wc -l` returns 0
2. `gh pr list --head feature/company-system --json number,state` returns an open PR
3. Phase 4 VERIFICATION.md truth 4 ("branch is pushed to remote in PR-ready state") is now satisfiable
</verification>

<success_criteria>
- Branch pushed: 0 commits ahead of origin
- PR exists on GitHub in open state
- GIT-03 requirement fully satisfied (both capability and delivery)
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-integration/04-04-SUMMARY.md`
</output>
