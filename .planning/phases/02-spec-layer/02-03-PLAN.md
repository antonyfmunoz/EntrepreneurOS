---
phase: 02-spec-layer
plan: "03"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/spec-parser/collaborative-flow.ts
  - lib/spec-parser/spec-editor.ts
  - .claude/skills/saas-dev/spec-parser/SKILL.md
  - tests/unit/spec-parser/collaborative-flow.test.ts
  - tests/unit/spec-parser/spec-editor.test.ts
autonomous: true
requirements: [SPEC-02, SPEC-05]
must_haves:
  truths:
    - "User can start with no spec and reach a complete PageSpec[] through domain-first structured questioning"
    - "Collaborative flow follows the sequence: vision -> user flows -> pages -> per-page detail -> implied requirements"
    - "References (URLs, screenshots, 'make it like X') are accepted at any point during collaboration"
    - "A user with no spec can invoke the spec-parser skill and produce a complete PageSpec[] through 5 structured domain-first questions"
    - "Surgical edits to a single page bump specVersion and flag dependent pages without full re-parse"
    - "Edit state transitions (spec-changed, needs-review) are implemented in code, not just skill prose"
  artifacts:
    - path: "lib/spec-parser/collaborative-flow.ts"
      provides: "Multi-turn collaborative spec creation state machine and types"
      exports: ["QUESTION_SEQUENCE", "CollaborativeState", "QuestionStage", "createInitialState", "buildSystemPromptForStage", "isFlowComplete", "extractSpecFromConversation"]
    - path: "lib/spec-parser/spec-editor.ts"
      provides: "Surgical spec editing with version bumping, dependency flagging, and provenance marking"
      exports: ["applySpecEdit", "flagDependentPages", "markProvenance"]
    - path: ".claude/skills/saas-dev/spec-parser/SKILL.md"
      provides: "Claude Code skill definition for spec-parser (orchestrates both paste and collaborate paths, chunking, confirmation gate with provenance display)"
    - path: "tests/unit/spec-parser/spec-editor.test.ts"
      provides: "Tests for surgical edit, version bumping, dependency flagging, and provenance marking"
  key_links:
    - from: "lib/spec-parser/collaborative-flow.ts"
      to: "shared/spec-schema.ts"
      via: "imports SpecOutputSchema for final validation"
      pattern: "SpecOutputSchema"
    - from: "lib/spec-parser/spec-editor.ts"
      to: "shared/spec-schema.ts"
      via: "imports PageSpecFull, SpecOutput for type safety"
      pattern: "PageSpecFull"
    - from: ".claude/skills/saas-dev/spec-parser/SKILL.md"
      to: "lib/spec-parser/parse-spec.ts"
      via: "skill instructs Claude Code to call parseSpec for paste path"
      pattern: "parseSpec"
    - from: ".claude/skills/saas-dev/spec-parser/SKILL.md"
      to: "lib/spec-parser/collaborative-flow.ts"
      via: "skill instructs Claude Code to use collaborative flow for no-spec path"
      pattern: "collaborative-flow"
    - from: ".claude/skills/saas-dev/spec-parser/SKILL.md"
      to: "lib/spec-parser/chunk-spec.ts"
      via: "skill instructs Claude Code to pre-chunk raw text before AI calls"
      pattern: "chunkRawText"
---

<objective>
Implement the collaborative spec creation flow (SPEC-02), surgical spec editing module with state transitions, and the spec-parser Claude Code skill definition that orchestrates both input paths with provenance-aware confirmation gate and pre-chunking.

Purpose: SPEC-02 requires users with no existing spec to reach a complete PageSpec[] through guided questioning. The spec-editor module implements edit state transitions in code (not just skill prose), addressing the MEDIUM-HIGH review concern. The skill definition ties all Phase 2 modules together into a single invocable Claude Code skill, with the updated chunking flow that pre-chunks raw text before AI calls (HIGH review concern).

Output: `lib/spec-parser/collaborative-flow.ts` with stateful questioning logic, `lib/spec-parser/spec-editor.ts` with surgical edit functions, `.claude/skills/saas-dev/spec-parser/SKILL.md` with complete skill definition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-spec-layer/02-CONTEXT.md
@.planning/phases/02-spec-layer/02-RESEARCH.md

@shared/spec-schema.ts
@lib/spec-parser/parse-spec.ts
@lib/spec-parser/restructure-spec.ts
@lib/spec-parser/derive-backend-spec.ts
@lib/spec-parser/deduplicate-components.ts
@lib/spec-parser/chunk-spec.ts
@.claude/skills/saas-dev/orchestrator/SKILL.md
@.claude/skills/saas-dev/detect-framework/SKILL.md
@.planning/phases/02-spec-layer/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 and Plan 02 — modules the skill orchestrates -->

From lib/spec-parser/parse-spec.ts:
```typescript
export const MAX_RAW_INPUT_SIZE: number;
export async function parseSpec(rawInput: string): Promise<SpecOutput>;
```

From lib/spec-parser/restructure-spec.ts:
```typescript
export async function restructureSpec(rawInput: string): Promise<SpecOutput>;
export function extractJsonFromResponse(text: string): unknown;
```

From lib/spec-parser/derive-backend-spec.ts:
```typescript
export async function deriveBackendSpec(pages: PageSpecFull[]): Promise<BackendSpec>;
```

From lib/spec-parser/deduplicate-components.ts:
```typescript
export async function deduplicateComponents(components: SharedComponentSpec[], pages: PageSpecFull[]): Promise<{ deduplicated: SharedComponentSpec[]; merges: Array<{ merged: string[]; into: string }> }>;
```

From lib/spec-parser/chunk-spec.ts:
```typescript
export function chunkRawText(rawText: string, maxChunkSize?: number): string[];
export function chunkSpecByDomain(pages: PageSpecFull[], chunkSize?: number): PageSpecFull[][];
```

From shared/spec-schema.ts:
```typescript
export const SpecItemSource = z.enum(["explicit", "inferred"]);
export const SpecOutputSchema = z.object({ pages: z.array(PageSpecFull).min(1), sharedComponents: z.array(SharedComponentSpec).default([]), suggestedOrder: z.array(z.string()).default([]), backendSpec: BackendSpecSchema.optional() });
export type PageSpecFull = z.infer<typeof PageSpecFull>;
export type SpecOutput = z.infer<typeof SpecOutputSchema>;
export type SpecItemSource = z.infer<typeof SpecItemSource>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collaborative spec creation flow and surgical spec editor modules</name>
  <files>lib/spec-parser/collaborative-flow.ts, lib/spec-parser/spec-editor.ts, tests/unit/spec-parser/collaborative-flow.test.ts, tests/unit/spec-parser/spec-editor.test.ts</files>
  <read_first>shared/spec-schema.ts, lib/spec-parser/restructure-spec.ts, lib/spec-parser/types.ts, .planning/phases/02-spec-layer/02-RESEARCH.md</read_first>
  <behavior>
    <!-- collaborative-flow.test.ts -->
    - Test: createInitialState returns state with stage "vision", stageIndex 0, empty messages, null partialSpec, complete false
    - Test: buildSystemPromptForStage("vision", "") returns prompt containing "product", "audience", "problem"
    - Test: buildSystemPromptForStage("user-flows", context) returns prompt containing "user journeys" or "core things a user does"
    - Test: buildSystemPromptForStage includes priorContext in the returned prompt
    - Test: isFlowComplete returns false when stageIndex < 5
    - Test: isFlowComplete returns true when stageIndex >= 5 and partialSpec is populated
    - Test: extractSpecFromConversation returns validated SpecOutput (mock Anthropic SDK)
    <!-- spec-editor.test.ts -->
    - Test: applySpecEdit replaces a page by route and bumps specVersion
    - Test: applySpecEdit throws when target page route not found in spec
    - Test: applySpecEdit preserves all other pages unchanged
    - Test: flagDependentPages returns routes of pages whose dependsOn includes the edited page's route
    - Test: flagDependentPages returns empty array when no pages depend on the edited page
    - Test: markProvenance marks all items in a SpecOutput — pages from user input as "explicit", AI-added pages as "inferred"
    - Test: markProvenance correctly compares against original input page names to determine explicit vs inferred
  </behavior>
  <action>
    **Write tests FIRST for both modules.**

    **Create `lib/spec-parser/collaborative-flow.ts`** implementing the domain-first questioning state machine (D-07).

    1. Define and export the question sequence as a const tuple:
       ```typescript
       export const QUESTION_SEQUENCE = [
         "vision",       // What is this product? Who is it for? What problem does it solve?
         "user-flows",   // What are the 3-5 core things a user does? Walk me through each.
         "pages",        // What pages/screens do those flows require? List them.
         "page-detail",  // For each page: what components, data, auth level?
         "implied",      // Review: what requirements are implied but not stated? (errors, empty states, auth gates, loading)
       ] as const;
       export type QuestionStage = typeof QUESTION_SEQUENCE[number];
       ```

    2. Define and export the `CollaborativeState` interface:
       ```typescript
       export interface CollaborativeState {
         stage: QuestionStage;
         stageIndex: number;
         messages: Array<{ role: "user" | "assistant"; content: string }>;
         references: string[];  // URLs, screenshots, "make it like X" references (D-08)
         partialSpec: Partial<SpecOutput> | null;
         complete: boolean;
       }
       ```
       **Note on persistence (addresses Gemini MEDIUM concern):** For v1, collaborative state lives in the Claude Code conversation context. The skill manages state through the QUESTION_SEQUENCE loop within a single session. Future versions could persist CollaborativeState to Neon's pipeline_runs table for cross-session resume.

    3. Export `function createInitialState(): CollaborativeState` — returns state with stage "vision", stageIndex 0, empty messages/references, null partialSpec, complete false.

    4. Export `function buildSystemPromptForStage(stage: QuestionStage, priorContext: string): string` — returns a system prompt for Claude that:
       - For "vision": asks about the product, target audience, core problem solved, revenue model
       - For "user-flows": asks user to describe 3-5 core user journeys step by step
       - For "pages": derives page list from user flows, asks for confirmation/additions
       - For "page-detail": iterates each page asking for components, data needs, auth level, layout preferences
       - For "implied": reviews all gathered info and identifies auth gates, error states, loading states, empty states, 404 handling, layout shell pages
       - Each prompt includes `priorContext` (serialized prior conversation summary) so Claude has full context
       - Each prompt instructs Claude to accept references at any point per D-08

    5. Export `function isFlowComplete(state: CollaborativeState): boolean` — returns true when stageIndex >= QUESTION_SEQUENCE.length and partialSpec is populated.

    6. Export `async function extractSpecFromConversation(messages: Array<{ role: "user" | "assistant"; content: string }>): Promise<SpecOutput>` that:
       - Sends the full conversation history to Claude with a system prompt instructing it to extract a complete SpecOutput JSON
       - **Provenance instruction:** System prompt tells Claude to mark items the user explicitly mentioned as `source: "explicit"` and items Claude inferred as `source: "inferred"`
       - Uses the same Anthropic SDK pattern as restructure-spec.ts
       - Validates with SpecOutputSchema.parse()
       - This is called at the end of the collaborative flow to produce the final output

    **Create `lib/spec-parser/spec-editor.ts`** implementing surgical edit state transitions (addresses MEDIUM-HIGH review concern: confirmation/edit flow gaps).

    This module puts edit logic in code, not just skill prose. D-16/D-17/D-18/D-19 require version bumping, dependency flagging, and provenance marking as real functions.

    1. Import PageSpecFull, SpecOutput types from `@shared/spec-schema.js`

    2. Export `function applySpecEdit(spec: SpecOutput, targetRoute: string, updatedPage: PageSpecFull): SpecOutput` that:
       - Finds the page in spec.pages where route === targetRoute
       - Throws if page not found: `"Page with route '${targetRoute}' not found in spec"`
       - Replaces the page with updatedPage, bumping specVersion to prev + 1 (D-16)
       - Returns new SpecOutput with the updated page (immutable — returns new object)
       - All other pages remain unchanged (D-18)

    3. Export `function flagDependentPages(spec: SpecOutput, editedRoute: string): string[]` that:
       - Scans all pages in spec.pages
       - Returns array of routes for pages whose `dependsOn` array includes editedRoute (D-17)
       - These pages should be flagged for review but not modified

    4. Export `function markProvenance(spec: SpecOutput, originalInputPageNames: string[]): SpecOutput` that:
       - For each page: if page.name is in originalInputPageNames, set source: "explicit"; otherwise set source: "inferred"
       - For shared components: if component.name was explicitly described by user, set source: "explicit"; otherwise "inferred"
       - Returns new SpecOutput with provenance marked (immutable)
       - This is called by the skill after restructuring to mark provenance based on comparing AI output against the user's original input

    **Important:** The actual multi-turn conversation loop is driven by the SKILL.md (which instructs Claude Code to iterate). These modules provide the building blocks; the skill orchestrates the interaction.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/spec-parser/collaborative-flow.test.ts tests/unit/spec-parser/spec-editor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/spec-parser/collaborative-flow.ts contains `export const QUESTION_SEQUENCE =`
    - lib/spec-parser/collaborative-flow.ts contains `export type QuestionStage =`
    - lib/spec-parser/collaborative-flow.ts contains `export interface CollaborativeState`
    - lib/spec-parser/collaborative-flow.ts contains `export function createInitialState(`
    - lib/spec-parser/collaborative-flow.ts contains `export function buildSystemPromptForStage(`
    - lib/spec-parser/collaborative-flow.ts contains `export function isFlowComplete(`
    - lib/spec-parser/collaborative-flow.ts contains `export async function extractSpecFromConversation(`
    - lib/spec-parser/collaborative-flow.ts contains `SpecOutputSchema.parse(`
    - lib/spec-parser/collaborative-flow.ts has 5 stages in QUESTION_SEQUENCE: vision, user-flows, pages, page-detail, implied
    - lib/spec-parser/collaborative-flow.ts contains a comment about v1 conversation context persistence
    - lib/spec-parser/spec-editor.ts contains `export function applySpecEdit(`
    - lib/spec-parser/spec-editor.ts contains `export function flagDependentPages(`
    - lib/spec-parser/spec-editor.ts contains `export function markProvenance(`
    - lib/spec-parser/spec-editor.ts applySpecEdit bumps specVersion (contains `specVersion` increment logic)
    - tests/unit/spec-parser/collaborative-flow.test.ts has at least 5 test cases
    - tests/unit/spec-parser/spec-editor.test.ts has at least 5 test cases
    - `npx vitest run tests/unit/spec-parser/collaborative-flow.test.ts` exits 0
    - `npx vitest run tests/unit/spec-parser/spec-editor.test.ts` exits 0
    - Existing tests still pass: `npx vitest run tests/unit/spec-parser/` exits 0
  </acceptance_criteria>
  <done>Collaborative flow module exports state machine with 5 questioning stages and provenance-aware extraction. Spec editor module implements surgical edits with version bumping, dependency flagging, and provenance marking as real code (not just skill prose). All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create spec-parser Claude Code skill definition with provenance display and pre-chunking flow</name>
  <files>.claude/skills/saas-dev/spec-parser/SKILL.md</files>
  <read_first>.claude/skills/saas-dev/orchestrator/SKILL.md, .claude/skills/saas-dev/detect-framework/SKILL.md, .planning/phases/02-spec-layer/02-CONTEXT.md</read_first>
  <action>
    Create `.claude/skills/saas-dev/spec-parser/SKILL.md` following the same YAML frontmatter + markdown body format as the existing skills.

    **Frontmatter:**
    ```yaml
    ---
    name: saas-dev:spec-parser
    description: Parses or collaboratively creates a SaaS product spec, producing a validated PageSpec[] with four composable layers (core, UI, data, analytics) plus shared components and backend spec. Supports paste-and-parse or domain-first guided creation. All items carry provenance (explicit vs inferred).
    ---
    ```

    **Body sections:**

    **# saas-dev:spec-parser**

    Ingests a product spec (pasted or created collaboratively) and produces a validated `SpecOutput` — a structured `PageSpec[]` with composable layers for downstream phases. Every item carries `source: "explicit" | "inferred"` provenance for confirmation display.

    **## Input Paths**

    1. **Paste path (SPEC-01):** User provides raw spec text (any format: markdown, plain text, Notion export, bullet list). System parses and restructures into PageSpec[].
       - **Pre-chunking (addresses oversized input):** Call `chunkRawText(rawInput)` from `lib/spec-parser/chunk-spec.ts` FIRST. If it returns multiple chunks, call `restructureSpec` on each chunk separately, then merge the SpecOutput results (combine pages[], merge sharedComponents via deduplicateComponents, unify suggestedOrder).
       - For single-chunk input: call `parseSpec(rawInput)` from `lib/spec-parser/parse-spec.ts` directly
       - Returns validated SpecOutput

    2. **Collaborate path (SPEC-02):** User has no spec. System guides through domain-first questioning.
       - Import `createInitialState`, `buildSystemPromptForStage`, `isFlowComplete`, `extractSpecFromConversation`, `QUESTION_SEQUENCE` from `lib/spec-parser/collaborative-flow.ts`
       - Initialize state with `createInitialState()`
       - For each stage in QUESTION_SEQUENCE:
         - Build system prompt with `buildSystemPromptForStage(stage, priorContext)`
         - Ask the user the stage-appropriate questions
         - Record user responses in state.messages
         - Accept references (URLs, screenshots, "make it like X") at any point (D-08)
       - After all stages: call `extractSpecFromConversation(state.messages)` to produce SpecOutput

    **## Post-Processing Pipeline**

    After either input path produces an initial SpecOutput:

    1. **Page count check (D-24/D-25):** If `output.pages.length > 25`, call `chunkSpecByDomain(pages)` from `lib/spec-parser/chunk-spec.ts`, re-restructure each chunk if needed, then merge.

    2. **Provenance marking:** Call `markProvenance(output, originalInputPageNames)` from `lib/spec-parser/spec-editor.ts` to ensure all items have accurate `source` fields based on comparison with the original user input.

    3. **Deduplication (D-20/D-21/D-22):** Call `deduplicateComponents(output.sharedComponents, output.pages)` from `lib/spec-parser/deduplicate-components.ts`. Show user the merges: "I merged these descriptions into one shared component: `ComponentName`." User confirms or splits.

    4. **Backend spec derivation (D-09/D-10):** Call `deriveBackendSpec(output.pages)` from `lib/spec-parser/derive-backend-spec.ts`. Auto-generation always runs when UI spec exists. If user also pastes a backend spec, show derived vs pasted and let user reconcile.

    5. **Backend-only concerns (D-12):** After auto-derivation, ask user: "Are there backend-only concerns not visible from the UI? (webhooks, cron jobs, third-party integrations, background tasks)" Append overrides to backendSpec.

    **## Confirmation Gate (D-03, D-04)**

    Before persisting ANYTHING to Neon:
    - Display the full restructured spec to the user
    - **Provenance display (addresses HIGH review concern):** For each page and component, display items with their `source` field:
      - Items with `source: "explicit"` — show normally, these came from the user's input
      - Items with `source: "inferred"` — prefix with `[INFERRED]` marker or use blockquote formatting to visually distinguish AI-added content from user-explicit content
    - Show the suggested generation order (D-15)
    - Show shared components with their deduplication merges (D-22)
    - Show the derived backend spec, with `source: "explicit"` endpoints from user input and `source: "inferred"` endpoints from AI derivation clearly marked
    - User options: "approve", "edit [page name]", or "redo" to re-interpret from scratch
    - On "edit": use `applySpecEdit` from `lib/spec-parser/spec-editor.ts` for surgical edit (D-16), which bumps specVersion automatically. Then call `flagDependentPages` to identify affected pages and re-show only changed + dependent pages for confirmation.
    - On "approve": persist to Neon pipeline_pages table

    **## Persistence**

    After user confirms:
    - Create pipeline_run row with phase="spec", status="complete"
    - For each page in SpecOutput.pages: insert pipeline_pages row with output=JSON.stringify(page), status="complete"
    - Store SpecOutput as the phase output for the orchestrator to hand off to Phase 3

    **Future consideration:** Collaborative flow state is currently held in conversation context. For cross-session resume, CollaborativeState could be serialized to pipeline_runs.output as JSON. Not implemented in v1.

    **## Spec Editing (D-16/D-17/D-18/D-19)**

    When user requests a spec edit after initial confirmation:
    - Accept page name or route to edit
    - Call `applySpecEdit(spec, targetRoute, updatedPage)` from `lib/spec-parser/spec-editor.ts` — this handles version bumping (D-16)
    - Call `flagDependentPages(spec, editedRoute)` — returns routes of pages that depend on the edited page (D-17)
    - Show confirmation gate for affected pages only (the edited page + flagged dependents)
    - Update pipeline_pages status to "spec-changed" for the edited page (D-19)
    - Pages with no relationship to the change are untouched (D-18)

    **## Contracts**

    - Input: raw text (any format) OR collaborative conversation
    - Output: `SpecOutput` from `shared/spec-schema.ts` — validated by `SpecOutputSchema.parse()`
    - Layers: PageSpecCore (all phases), PageSpecUI (Phase 3-4), PageSpecData (Phase 5), PageSpecAnalytics (Phase 6)
    - Composition: Downstream phases use `PageSpecCore.merge(PageSpecUI)` etc. to get only their fields
    - Provenance: Every page, component, endpoint, and event carries `source: "explicit" | "inferred"`

    **## Dependencies**

    - `lib/spec-parser/parse-spec.ts` — paste path entry point (with size guard)
    - `lib/spec-parser/restructure-spec.ts` — AI restructuring engine (with provenance in system prompt)
    - `lib/spec-parser/collaborative-flow.ts` — guided creation state machine
    - `lib/spec-parser/spec-editor.ts` — surgical editing with version bumping and dependency flagging
    - `lib/spec-parser/derive-backend-spec.ts` — backend spec auto-derivation (with provenance propagation)
    - `lib/spec-parser/deduplicate-components.ts` — shared component dedup (with provenance preservation)
    - `lib/spec-parser/chunk-spec.ts` — large spec chunking (both raw text pre-chunking and parsed page post-chunking)
    - `shared/spec-schema.ts` — all Zod contracts including SpecItemSource

    **## Sub-Skill Of**

    `saas-dev:orchestrator` (phase: spec)

    Also update `.claude/skills/saas-dev/orchestrator/SKILL.md`:
    - Under "Current Sub-Skills", add: `- saas-dev:spec-parser`
    - Update the "spec" phase description to reference: "routes to saas-dev:spec-parser — see `.claude/skills/saas-dev/spec-parser/SKILL.md`"
  </action>
  <verify>
    <automated>cat .claude/skills/saas-dev/spec-parser/SKILL.md | head -5 && cat .claude/skills/saas-dev/orchestrator/SKILL.md | grep "spec-parser"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/saas-dev/spec-parser/SKILL.md exists
    - .claude/skills/saas-dev/spec-parser/SKILL.md contains `name: saas-dev:spec-parser`
    - .claude/skills/saas-dev/spec-parser/SKILL.md contains `## Input Paths`
    - .claude/skills/saas-dev/spec-parser/SKILL.md contains `chunkRawText` in the paste path section (pre-chunking before AI)
    - .claude/skills/saas-dev/spec-parser/SKILL.md contains `## Confirmation Gate`
    - .claude/skills/saas-dev/spec-parser/SKILL.md contains `[INFERRED]` or `source: "inferred"` in confirmation gate section
    - .claude/skills/saas-dev/spec-parser/SKILL.md contains `applySpecEdit` reference for surgical editing
    - .claude/skills/saas-dev/spec-parser/SKILL.md contains `flagDependentPages` reference
    - .claude/skills/saas-dev/spec-parser/SKILL.md contains `markProvenance` reference
    - .claude/skills/saas-dev/spec-parser/SKILL.md contains `parseSpec` reference
    - .claude/skills/saas-dev/spec-parser/SKILL.md contains `collaborative-flow` reference
    - .claude/skills/saas-dev/spec-parser/SKILL.md contains `deriveBackendSpec` reference
    - .claude/skills/saas-dev/spec-parser/SKILL.md contains `deduplicateComponents` reference
    - .claude/skills/saas-dev/spec-parser/SKILL.md contains `SpecOutputSchema` reference
    - .claude/skills/saas-dev/spec-parser/SKILL.md contains `SpecItemSource` or provenance reference
    - .claude/skills/saas-dev/orchestrator/SKILL.md contains `saas-dev:spec-parser`
  </acceptance_criteria>
  <done>spec-parser skill definition documents both input paths with pre-chunking flow, provenance-aware confirmation gate displaying explicit vs inferred items, surgical editing via spec-editor module, and all post-processing steps. Orchestrator skill updated to reference spec-parser as a sub-skill.</done>
</task>

</tasks>

<verification>
npx vitest run tests/unit/spec-parser/
npx vitest run
cat .claude/skills/saas-dev/spec-parser/SKILL.md | head -3
</verification>

<success_criteria>
- Collaborative flow module exports complete state machine with 5 questioning stages and provenance-aware extraction
- Spec editor module exports applySpecEdit (version bumping), flagDependentPages, and markProvenance as tested code
- spec-parser SKILL.md documents pre-chunking flow (chunkRawText FIRST for paste path)
- spec-parser SKILL.md documents provenance display in confirmation gate (explicit vs inferred markers)
- spec-parser SKILL.md documents surgical editing via spec-editor module functions
- Confirmation gate (D-03/D-04) clearly documented with provenance-based visual distinction
- Orchestrator SKILL.md references spec-parser as a sub-skill
- All existing tests remain passing
</success_criteria>

<output>
After completion, create `.planning/phases/02-spec-layer/02-03-SUMMARY.md`
</output>
