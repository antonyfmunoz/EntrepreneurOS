// AUTO-GENERATED by saas-dev:backend-wirer — do not edit manually
import { eq } from "drizzle-orm";
import { db } from "../../db";
import { register, type Register, type InsertRegister } from "../../../shared/schema";

export const registerStorage = {
    async createRegister(data: InsertRegister): Promise<Register> {
      const id = crypto.randomUUID();
      const [created] = await db.insert(register).values({ id, ...data }).returning();
      return created;
    }
};
