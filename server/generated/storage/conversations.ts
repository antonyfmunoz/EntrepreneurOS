// AUTO-GENERATED by saas-dev:backend-wirer — do not edit manually
import { eq } from "drizzle-orm";
import { db } from "../../db";
import { conversations, type Conversation, type InsertConversation } from "../../../shared/schema";

export const conversationsStorage = {
    async createConversation(data: InsertConversation): Promise<Conversation> {
      const id = crypto.randomUUID();
      const [created] = await db.insert(conversations).values({ id, ...data }).returning();
      return created;
    }
};
