// AUTO-GENERATED by saas-dev:backend-agent — do not edit manually
import { eq } from "drizzle-orm";
import { db } from "../../db";
import { logout, type Logout, type InsertLogout } from "../../../shared/schema";

export const logoutStorage = {
    async createLogout(data: InsertLogout): Promise<Logout> {
      const id = crypto.randomUUID();
      const [created] = await db.insert(logout).values({ id, ...data }).returning();
      return created;
    }
};
