// AUTO-GENERATED by saas-dev:backend-wirer — do not edit manually
import { db } from "../../db";
import { login } from "../../../shared/schema";

export const loginStorage = {
    async createLogin(data: InsertLogin): Promise<Login> {
      const id = crypto.randomUUID();
      const [created] = await db.insert(loginTable).values({ id, ...data }).returning();
      return created;
    }
};
