// AUTO-GENERATED by saas-dev:backend-agent — do not edit manually
import { eq } from "drizzle-orm";
import { db } from "../../db";
import { login, type Login, type InsertLogin } from "../../../shared/schema";

export const loginStorage = {
    async createLogin(data: InsertLogin): Promise<Login> {
      const id = crypto.randomUUID();
      const [created] = await db.insert(login).values({ id, ...data }).returning();
      return created;
    }
};
