export function integrationBindingConfigurationSnapshot(record: any) {
  return {
    bindingId: record.id,
    integrationKey: record.integrationKey,
    configurationVersion: record.configurationVersion,
    providerKey: record.providerKey,
    providerAccountReference: record.providerAccountReference,
    adapterKind: record.adapterKind,
    adapterReference: record.adapterReference,
    adapterVersion: record.adapterVersion,
    transport: record.transport,
    administratorReference: record.administratorReference,
    recoveryOwnerSeatId: record.recoveryOwnerSeatId,
    accountScope: record.accountScope,
    nativePermissions: record.nativePermissions,
    credentialReference: record.credentialReference,
    executionAuthority: record.executionAuthority,
    operations: record.operations,
    expectedEvents: record.expectedEvents,
    inputSchema: record.inputSchema,
    outputSchema: record.outputSchema,
    eventSchema: record.eventSchema,
    costModel: record.costModel,
    latencyBudgetMs: record.latencyBudgetMs,
    rateLimitPolicy: record.rateLimitPolicy,
    idempotencyStrategy: record.idempotencyStrategy,
    retryPolicy: record.retryPolicy,
    timeoutMs: record.timeoutMs,
    cancellationBehavior: record.cancellationBehavior,
    redactionPolicy: record.redactionPolicy,
    evidenceRequirements: record.evidenceRequirements,
    testCapability: record.testCapability,
    revocationProcedure: record.revocationProcedure,
    manualFallback: record.manualFallback,
    failureRecovery: record.failureRecovery,
    replacementStatus: record.replacementStatus,
    parityState: record.parityState,
    lifecycleState: record.lifecycleState,
    connectionState: record.connectionState,
    healthState: record.healthState,
    evidenceIds: record.evidenceIds,
    schemaVersion: record.schemaVersion,
  };
}
